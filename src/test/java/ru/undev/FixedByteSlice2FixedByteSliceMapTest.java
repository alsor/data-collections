package ru.undev;

import org.junit.Test;

public class FixedByteSlice2FixedByteSliceMapTest {

	@Test
	public void store_bytes() throws Exception {
		// int nextValueOffset = map.putKey(src, offset, length);
		// nextValueOffset = map.putIntVal(valOffset, val1);
		// map.putIntVal(valOffset, val2);
		//
		// int nextValueOffset = map.getKey(src, offset, length);
		// assertTrue(nextValueOffset >=0);
		// int foundVal1 = map.getIntVal
		// assertTrue()
		
	}

}
